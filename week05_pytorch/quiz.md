# Week 05 Quiz

1. `model.eval()`의 목적은?
2. gradient 계산을 끄면 추론에 어떤 이점이 있는가?
3. preprocessing이 학습 때와 다르면 어떤 문제가 생기는가?
4. 평균 latency와 P95 latency를 함께 보는 이유는?
5. batch size 증가가 latency/throughput에 미칠 수 있는 영향을 설명하라.