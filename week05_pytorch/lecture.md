# Week 05 Lecture — PyTorch Inference

## 핵심 개념
Tensor, device, model inference, batch, preprocessing을 다룬다.

## 학습 포인트
- CPU Tensor와 CUDA Tensor
- `model.eval()`
- `torch.no_grad()` 또는 inference mode
- 입력 정규화와 모델 정확도
- 평균 latency, P95 latency의 차이